sudo: false

language: node_js

env:
  global:
    - PORT=3000
matrix:
  include:
    - node_js: "8"

after_script: "npm install coveralls && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
notifications:
  email:
    - i@tadatuta.com
